
  import 'package:flutter/material.dart';
  import 'package:flutter/material.dart';

  class AppColors {
    // Primary Colors
    static const primary = Color(0xFFF4511E);
    static const subColor = Color(0xFf686B70);
    static const borderColor = Color(0xFFE0E6ED);
    static const backGround = Color(0xFFF9FAFB);
    // Status Colors
    static const green = Color(0xFF15D031);
    static const red = Color(0xFFF44336);
    static const yellow = Color(0xFFEFA947);
    static const blue = Color(0xFF2196F3);
    // Text & Grey
    static const black = Color(0xFF000000);
    static const white = Color(0xFFFFFFFF);
    static const grey = Color(0xFF585858);
    static final lightGrey = Colors.grey[500];
    static const darkGrey = Color(0xFF2C2C2C);

    // Shadows & Misc
    static const shadowGrey = Color.fromRGBO(245, 245, 245, 0.3);

    static const purple = Color(0xFF9C27B0);
    static const purpleLight = Color(0xFFE1BEE7);
  }

